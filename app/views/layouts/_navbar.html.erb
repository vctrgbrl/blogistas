<nav class="navbar navbar-expanded-lg bg-body-tertiary">
  <div class="container container-fluid">
    <div class="container d-flex justify-content-between">
      <div class="">
        <%= link_to "Blogistas", root_path, class: "navbar-brand" %>
      </div>
      <div class="d-flex">
        <% if user_signed_in? %>
          <%= link_to "Nova Publicação", new_post_path %>
          <%= link_to "Perfil", edit_user_registration_path, class: "link-primary" %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "link-primary" %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class: "link-primary" %>
          <%= link_to "Sign In", new_user_session_path, class: "link-primary" %>
        <% end %>
      </div>

    </div>
  </div>
</nav>